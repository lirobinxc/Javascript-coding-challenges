function checkDriverAge(age) {
    if (age < 18) {
        return("Too young to drive.")
    } 
    else if (age === 18) {
        return("Congrats on your first year of driving!");
    } 
    else if (age > 18) {
        return("Powering car on, have fun, old timer.")
    }
}

        